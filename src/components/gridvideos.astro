<section class="px-5 pt-16 max-w-6xl m-auto">
    <div class="grid-videos">
        <video
            src="/videos/95010-644716378_medium.mp4"
            muted
            loop
            onmouseover="this.play()"
            onmouseout="this.pause(); this.currentTime = 0;"></video>
        <video
            src="/videos/277468_small.mp4"
            muted
            loop
            onmouseover="this.play()"
            onmouseout="this.pause(); this.currentTime = 0;"></video>
        <video
            src="/videos/252125_medium.mp4"
            muted
            loop
            onmouseover="this.play()"
            onmouseout="this.pause(); this.currentTime = 0;"></video>
    </div>
</section>
<style>
    section {
        position: relative;
    }
    .grid-videos {
        display: grid;
        grid-template-columns: 1fr 400px;
        grid-template-rows: repeat(2, 300px);
        gap: 20px;
    }
    .grid-videos video:nth-child(2) {
        grid-row: span 2;
    }
    video {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 16px;
    }
</style>

